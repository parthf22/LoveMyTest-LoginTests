{
  "name": "lovemytest-login-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for LoveMyTestOnline login page",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --project=chromium --headed",
    "test:chrome": "playwright test --project=chromium",
    "test:firefox": "playwright test --project=firefox",
    "test:report": "playwright show-report",
    "generate-report": "node reports/generate-report.js",
    "generate-pdf": "node reports/generate-pdf.js",
    "run-all": "playwright test --project=chromium --headed && node reports/generate-report.js && node reports/generate-pdf.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
